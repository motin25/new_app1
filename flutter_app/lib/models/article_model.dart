class ArticleModel{
  String author;
  String title;
  String description;
  String url;
  String urlToImage;
  String content;
  String articleUrl;
  DateTime publshedAt;


  ArticleModel({this.author, this.title, this.description, this.url, this.content, this.urlToImage, this.publshedAt, this.articleUrl});
}